#include<stdio.h>
void main()
{
    int i,n,p;
    int a[n];
    printf("\n how many elements");
    scanf("%d",&n);
    for(i=0;i<n;i++)
        a[i]=(p+1)*(p+1);
    printf("\n array is");
    for(i=0;i<n;i++)
    printf("%4d",a[i]);
}